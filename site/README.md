# toolars site

All production application code for toolars belongs in this directory.

Planned stack:

- Next.js App Router
- TypeScript
- Tailwind CSS plus shadcn-style primitives
- Vitest and Playwright

Do not place application code at the repository root, in `docs/`, or in
`design/`.

