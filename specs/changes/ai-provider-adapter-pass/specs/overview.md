# Specs Overview: ai-provider-adapter-pass

This change is design-only. It prepares the production AI provider boundary for
Toolars without adding runtime dependencies or external API calls.

Capabilities:

- `ai-provider-adapter`: provider-neutral generation contract, streaming,
  errors, telemetry, and migration plan.
