# Specs Overview: auth-db-production-design

This change is design-only. It prepares W2 Real SaaS Backend work by defining
the production Auth and Postgres plan for Toolars.

Capabilities:

- `auth-db-architecture`: Supabase SSR session model, database schema,
  RLS/service boundaries, and migration plan from preview auth.
