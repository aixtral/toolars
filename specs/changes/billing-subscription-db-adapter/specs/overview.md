# Specs Overview: billing-subscription-db-adapter

This change implements the durable billing subscription database adapter for
Toolars W2 backend work.

Covered capability:

- `billing-subscription-db-adapter`: Supabase tables, server-only adapter,
  idempotent event recording, subscription upsert, and webhook runtime
  injection.
