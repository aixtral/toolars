# Spec Overview: security-event-logging-pass

This change closes M3 from `docs/security/SECURITY-AUDIT-RELEASE-GATE.md` by
adding safe server-side security events for AI and billing route failures.

Capabilities:

- `security-event-logging`: structured non-sensitive event recording for
  security-relevant AI and billing failures.
